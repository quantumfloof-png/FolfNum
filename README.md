# FolfNum (A LARGE number library, built for speed and performance (regularly updated.) **READ DESCRIPTION** )
FolfNum is an abritrary number module.. specifically inclusive of suffixes and extremely easy to use. Documentation will be released on further notice.

Some of the things you are able to do with FolfNum (V0.5) currently are.. 

Incremental Games.. you are able to use this module to handle very large numbers and not have it bloated with unnecessary functions. This module will be **significantly** updated with performance changes and speed calculations.. potentially a future conversion into C++ or C.

# **LATENCY/SPEED CALCULATIONS (AVERAGE OF 500,000 ITERATIONS)**

**add(a, b)** = ~3.359 µs
**sub(a, b)** = ~2.818 µs
**mul(a, b)** = ~2.926 µs
**div(a, b)** = ~2.268 µs
**gt(a, b)** = ~332 ns
**lt(a, b)** = ~583 ns

import FolfNum after it is in the root directory as "from FolfNum import *"

This module is not entirely optimized for speed and/or performance yet as I just began learning python on 8/16/2024 at ~6:30PM and I am nowhere near exceptional/advanced yet. I just pick up things quickly. :3